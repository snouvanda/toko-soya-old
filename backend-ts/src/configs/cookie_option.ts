export const cookieStandarOption = {
  httpOnly: true,
  secure: false,
  maxAge: 24 * 60 * 60 * 1000,
}

export const cookieSecureOption = {
  httpOnly: true,
  secure: true,
  maxAge: 24 * 60 * 60 * 1000,
}
